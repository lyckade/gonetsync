//Package filelist is the representation of all the files of a directory tree
package filelist

import (
	"fmt"

	"github.com/lyckade/gonetsync/file"
)

//GenerateList creates a fileInfo list
func GenerateList(folder string) file.Info {
	var fi file.Info
	fmt.Println("test")
	return fi
}
